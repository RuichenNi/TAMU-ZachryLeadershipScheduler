module ApplicationHelper
    #The methods in the helpers are actually view helper methods, we mainly use them in the corresponding view
    #If we want to use these method in the corresponding controller, we need to use by such way: helpers.method_name(params)
end
